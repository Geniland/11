<form method="post" action="{{ path('app_new_femmes_delete', {'id': new_femme.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ new_femme.id) }}">
    <button class="btn">Delete</button>
</form>
