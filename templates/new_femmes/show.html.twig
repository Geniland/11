{% extends 'base.html.twig' %}

{% block title %}NewFemmes{% endblock %}

{% block body %}
    <h1>NewFemmes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ new_femme.id }}</td>
            </tr>
            <tr>
                <th>FemmeImage</th>
                <td>{{ new_femme.FemmeImage }}</td>
            </tr>
            <tr>
                <th>FemmeNom</th>
                <td>{{ new_femme.FemmeNom }}</td>
            </tr>
            <tr>
                <th>FemmePrix</th>
                <td>{{ new_femme.FemmePrix }}</td>
            </tr>
            <tr>
                <th>FemmeEtat</th>
                <td>{{ new_femme.FemmeEtat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_new_femmes_index') }}">back to list</a>

    <a href="{{ path('app_new_femmes_edit', {'id': new_femme.id}) }}">edit</a>

    {{ include('new_femmes/_delete_form.html.twig') }}
{% endblock %}
